
clazz.getFactory()
    .setMetaProcessor(meta('/ClazzJS/Base'))
    .setBaseClazz(clazz('/ClazzJS/Base'));

return {
    Namespace: Namespace,
    Clazz:     Clazz,
    Meta:      Meta,

    namespace: namespace,
    clazz:     clazz,
    meta:      meta
};

}));